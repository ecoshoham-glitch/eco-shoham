# העתיקו לקובץ .env.local (לא ב-git, כבר ב-.gitignore) וקבעו ערך אמיתי.
# בלי VITE_ADMIN_PASSWORD, החשבון admin@ecoshoham.co.il חסום לגמרי (לא נכשל
# בשקט לסיסמה ריקה) — ראו src/api/base44Client.js.
#
# חשוב: זו לא הגנה אמיתית. כל משתנה שמתחיל ב-VITE_ נארז לתוך ה-bundle
# הציבורי בזמן build ונגיש לכל מי שפותח את קוד המקור של האתר בדפדפן —
# בדיוק כמו סיסמה שכתובה ישירות בקוד. זה משפר רק את היגיינת ה-git
# (הסיסמה לא נשמרת לצמיתות בהיסטוריית git, וניתן להחליף אותה בלי commit).
VITE_ADMIN_PASSWORD=
